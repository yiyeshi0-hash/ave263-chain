# 26.3 ANE (AppleH11ANEInterface) 张量尺寸 mul 候选（2026-08-17）

## ★ 强候选：函数 0xfffffff008edc5bc（张量描述解析）

### 完整调用链分析
```
0x8edc5bc(x0=this, x1, x2=x24=张量描述, x3=x22, x4=x19):
  ldr x0, [x0, 8]; vtable [0x10] 调用 → x25 = 已有张量数
  [x21+0x38] vtable 调用 → 检查返回
  cmp x25, x23        ; x25=已有, x23=新数量 — 只查数量
  b.ls → 0x8edc698    ; 数量合法 → 进入维度计算

0x8edc698 (维度→元素数):
  ldrh w8,  [x21, 0x10]    ; 用户字段
  ldrh w9,  [x24]          ; 张量维度[0] (16位, 用户可控)
  orr  w10, w8, w9, lsl 16
  ldrh w28, [x24, 2]       ; 维度[1]
  ldrh w26, [x24, 4]       ; 维度[2]
  ldrh w27, [x24, 6]       ; 维度[3]
  ldrh w25, [x24, 8]       ; 维度[4]
  mul  w8, w25, w27        ; ★ 32位: dim4×dim3
  mul  w8, w8, w26         ; ★ ×dim2
  mul  w8, w8, w28         ; ★ ×dim1
  str  w8, [sp, 0x10]      ; 元素数(32位)
  ...
  str  w8, [x20, 0x10]     ; ← 元素数存入对象
  blraa x24 (回调) → 逐个维度处理
```

### 漏洞机制
- **5 个 16 位维度 × 3 次 32 位连乘**：65535⁴ ≈ 2⁶⁴ ≫ 2³² → **必然回绕**
- 维度数组 [x24] 来自**用户提交的 ANE 张量/模型描述** → 用户可控
- 前置检查只验**数量**（x25 vs x23），**不验维度值大小**
- 回绕后元素数存对象 [0x10] → **后续分配/索引/遍历用错误元素数 → 内核 OOB**

### 可控性前提
- 越狱机：直接 IOKit 调 ANE IOUserClient（注入 entitlement）
- 未越狱：CoreML/ANECompiler 用户态可能校验维度（待实测）
- **用户明确接受越狱前提** → 此候选值得深挖

### 待验证
1. [x24] 张量描述的确切来源（IOExternalMethod 参数？对象方法？）
2. 26.6 H16 ANE 同函数是否修复（smull/检查）
3. 元素数后续用途（分配？索引？）——决定 OOB 类型
4. 真机验证路径（越狱后直连 ANE）
